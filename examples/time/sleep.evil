@ Shows the sleep function in the time domain @

use system
use time

[
    version 2.0
    output sleep
    os linux
    arch amd64
]

l main {
    time::sleep("10")
    system::out("It's gone 10s since I was launched!")
    system::exit("0")
}