@ Shutdowns the computer @

use system

[
    version 2.0
    output shutdown
    os linux
    arch amd64
    obfuscate false
]

b reboot {
    system::shutdown()
}

@ These function will never be called @
l main_func {
    system::out("hello, world!")
    system::exit("0")
}