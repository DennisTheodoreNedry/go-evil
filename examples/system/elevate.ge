@ Not the best example, but it tries to showcase how an elevation works

[version 1.2]
use system;

main:{
    system.io.out("Current user: €666"); @ We can use €666 to tell us which is the current running user
    system.elevate(); @ Tries to gain elevated rights on the system
    system.io.out("Response from the subprocess €1");
    system.exit("0");
}