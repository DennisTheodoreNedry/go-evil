@ Showcases how a for-loop/foreach-loop works @

use system

[
    version 2.0
    output foreach
    os linux
    arch amd64
    obfuscate false
]


l main_func {
    foreach(${"1", "2", "3", "4", "5"}$):
        system::outln("€42€") @ The index €42€ is configured to only be used by foreach loops @
    end foreach
    
    system::exit("0")
}