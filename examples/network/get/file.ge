@ Examples shows how we can scrape a website and save the result in
@ a local file

[version 1.1]

main:{
    #net.get.save_disk(); @ Tells the system that we want to save the result to a runtime variable
    #net.get("mrskeltal.com"); @ The target
    system.out("€1"); @ This will print the file name
    system.exit("0");
}