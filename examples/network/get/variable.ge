@ Examples shows how we can scrape a website and save the result in
@ a runtime variable

[version 1.2]
use system;
use network;

main:{
    #net.get.save_variable(); @ Tells the system that we want to save the result to a runtime variable
    #net.get("mrskeltal.com"); @ The target
    system.io.out("€1"); @ And here lies the output
    system.exit("0");
}